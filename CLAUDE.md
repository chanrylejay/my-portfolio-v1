# my-portfolio-v1

Chan's personal portfolio site (chanryle-cagara.vercel.app). Plain HTML/CSS/JS, no build step. Vercel auto-deploys from main, so push = live site change and needs GO.
Run: open index.html directly, or `python -m http.server` for a local server.
Global context about Chan and the working contract auto-loads from the global hub. Project-only facts live in this project's MEMORY.md. Keep leanctx-seed.js in the root forever.
